{
  "name": "reciprocity-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "rescript build && vite --host 0.0.0.0",
    "build": "rescript build && vite build",
    "build:pages": "rescript build && vite build --base=/reciprocity-tracker/",
    "check": "rescript build && node --test tests/state.test.mjs"
  },
  "dependencies": {
    "@rescript/react": "^0.15.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "rescript": "^12.3.1",
    "vite": "^8.2.0"
  }
}
